#include "cards.h"

int main(void) {
  print_card(card_from_num(51));
  // print_card(card_from_letters('2', 's'));
  // print_card(card_from_letters('J', 'd'));
  // print_card(card_from_letters('A', 'h'));
}
